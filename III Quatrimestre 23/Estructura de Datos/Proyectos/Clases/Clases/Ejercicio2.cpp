/*Construya una clase llamada Rectangulo que tenga los siguientes atributos Largo y Ancho
y los siguientes metodos perimetro () y area ()*/

/*
#include <iostream>
#include <stdlib.h>
using namespace std;

class Rectangulo {
private:
	float largo;
	float ancho;

public:
	Rectangulo(float, float);
	void perimetro();
	void area();
};

Rectangulo::Rectangulo(float _largo, float _ancho) {
	largo = _largo;
	ancho = _ancho;
}

//Metodo Perimetro
void Rectangulo::perimetro() {
	float _perimetro;
	_perimetro = (2 * largo) + (2 * ancho);
	cout << " El Perimetro es: " << _perimetro << endl;

}
//Metodo Area
void Rectangulo::area() {
	float _area;
	_area = largo * ancho;
	cout << " El Area es: " << _area << endl;
}
int main() {
	Rectangulo r1(11, 7);
	Rectangulo r2(12.6, 8.4);

	r1.perimetro();
	r2.area();

	system("pause");
	return 0;
}*/